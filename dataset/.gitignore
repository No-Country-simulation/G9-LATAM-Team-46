# No versionar datos crudos ni procesados (pueden pesar cientos de MB/GB)
raw/*
processed/*
!raw/.gitkeep
!processed/.gitkeep
# Excepción: el dataset bilingüe final SÍ se versiona para que todo el equipo lo tenga
!processed/techmind_dataset_v2.csv
!processed/README.md

# Cache local de kagglehub si se llega a generar dentro del repo
kagglehub_cache/

# Checkpoints de Jupyter
notebooks/.ipynb_checkpoints/
