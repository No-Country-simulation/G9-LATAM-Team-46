__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# NOTA: los artefactos de models/ SÍ se versionan, la API los toma del
# repo. El .joblib pesa 5,9 MB y matriz_historica.pkl 47 MB, los dos por
# debajo del límite de 100 MB de GitHub, así que no hace falta Git LFS.
# GitHub avisa a partir de los 50 MB: si la matriz crece, habrá que
# moverla a LFS o a almacenamiento externo.
